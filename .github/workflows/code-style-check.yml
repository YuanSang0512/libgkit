name: gkit-code-style-check

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  clang-format:
    name: Clang-Format Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run clang-format
        uses: Jayllyz/clang-format-action@v1.3.0
        with:
          check: true
          style: file
          extensions: cpp,hpp
          clang-version: latest

  clang-tidy:
    name: Clang-Tidy Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: "recursive"

      - name: Install dependencies
        run: |
          echo "📦 Installing Ubuntu dependencies..."
          sudo apt-get update
          sudo apt-get install -y \
            build-essential cmake ninja-build wget software-properties-common \
            libasound2-dev libpulse-dev libx11-dev libxext-dev \
            libxrandr-dev libxcursor-dev libxfixes-dev libxi-dev \
            libxss-dev libxtst-dev libxkbcommon-dev libdrm-dev \
            libgbm-dev libgl1-mesa-dev libgles2-mesa-dev libegl1-mesa-dev \
            libdbus-1-dev libibus-1.0-dev libudev-dev

          echo "📦 Installing latest clang toolchain from LLVM apt..."
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          LLVM_LATEST=$(curl -sL 'https://apt.llvm.org/noble/dists/llvm-toolchain-noble/' \
            | grep -oE 'llvm-toolchain-noble-[0-9]+' | sed 's/.*-//' | sort -n | tail -1)
          echo "Latest LLVM version: ${LLVM_LATEST}"
          sudo add-apt-repository -y "deb http://apt.llvm.org/noble/ llvm-toolchain-noble-${LLVM_LATEST} main"
          sudo apt-get update
          sudo apt-get install -y clang-${LLVM_LATEST} clang-tidy-${LLVM_LATEST}
          sudo update-alternatives --install /usr/bin/clang clang /usr/bin/clang-${LLVM_LATEST} 100
          sudo update-alternatives --install /usr/bin/clang++ clang++ /usr/bin/clang++-${LLVM_LATEST} 100
          sudo update-alternatives --install /usr/bin/clang-tidy clang-tidy /usr/bin/clang-tidy-${LLVM_LATEST} 100
          echo "✅ clang $(clang --version | head -n1) installed"
          echo "✅ clang-tidy $(clang-tidy --version | head -n1) installed"

      - name: Configure CMake (LLVM)
        run: |
          echo "⚙️ Configuring CMake with LLVM preset..."
          cmake --preset linux-x86_64-llvm-debug
          echo "✅ CMake configuration completed"

      - name: Run clang-tidy
        run: |
          echo "🔍 Running clang-tidy..."
          COMPILE_DB="${{ github.workspace }}/build/linux-x86_64-llvm-debug/compile_commands.json"

          if [ ! -f "$COMPILE_DB" ]; then
            echo "::error::compile_commands.json not found at $COMPILE_DB"
            exit 1
          fi

          FILES=$(find "${{ github.workspace }}/src" -name "*.cpp" | sort)

          EXIT_CODE=0
          for file in $FILES; do
            echo "  Checking: $file"
            if ! clang-tidy "$file" -p "$(dirname "$COMPILE_DB")" --quiet 2>&1; then
              EXIT_CODE=1
            fi
          done

          if [ $EXIT_CODE -ne 0 ]; then
            echo "::error::clang-tidy found issues"
            exit 1
          fi

          echo "✅ clang-tidy check passed"
